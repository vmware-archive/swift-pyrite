require 'spec_helper'

describe Swift::Pyrite do
  it 'has a version number' do
    expect(Swift::Pyrite::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
